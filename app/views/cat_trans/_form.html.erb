<%= form_with(model: cat_tran) do |form| %>
  <% if cat_tran.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(cat_tran.errors.count, "error") %> prohibited this cat_tran from being saved:</h2>

      <ul>
        <% cat_tran.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
